<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scd.myspa" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_spa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_spa_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySPA"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.ActivityMain"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.MySPA.NoActionBar" />
        <activity
            android:name=".ui.ActivityLog"
            android:exported="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>